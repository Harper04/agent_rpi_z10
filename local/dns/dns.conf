# DNS sync configuration — machine-specific.
# See docs/recipes/route53-dns.md for setup instructions.

OWNER_TAG="s85"            # unique per machine (e.g. hostname -s)
DEFAULT_TTL=300

# Interface-to-FQDN mapping for dynamic DNS updates (optional).
# Format: "iface=fqdn iface=fqdn ..."
# Use + suffix for prefix matching (e.g. zt+ matches zt0, ztly7nnh6j)
IP_RECORD_MAP="ztrta4apg4=s85.zt.tiny-systems.eu eth0=s85.local.tiny-systems.eu"
