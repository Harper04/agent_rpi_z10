OWNER_TAG="ziegeleiweg-pi"
DEFAULT_TTL=300

# Interface-to-FQDN mapping for dynamic DNS updates.
# Format: "iface=fqdn iface=fqdn ..."
# Use + suffix for prefix matching (e.g. zt+ matches zt0, ztly7nnh6j, etc.)
IP_RECORD_MAP="br0=z10.local.tiny-systems.eu zt+=z10.zt.tiny-systems.eu"
